# v1.0.6 更新日志

**发布日期**: 2025-12-15

## 🎯 核心更新

### 1. 修复 Skill 安装问题

**问题**: 用户通过 `npx @waoooo/genkicap-workflow` 初始化工作空间时，`.claude/skills/` 目录没有被复制，导致 `project-manager` skill 无法使用。

**修复**:
- 修改 `lib/modules/init.js`，添加 skills 目录复制逻辑
- 用户初始化后，`.claude/skills/project-manager/` 会被正确安装

**影响文件**:
- `v1.0/lib/modules/init.js` (第 553-560 行)

**修改内容**:
```javascript
// Copy skills directory
if (fs.existsSync(path.join(packageRoot, '.claude', 'skills'))) {
  copyDir(
    path.join(packageRoot, '.claude', 'skills'),
    path.join(claudeDir, 'skills'),
    []
  );
}
```

---

### 2. 统一中文文件命名规范

**问题**:
- `i18n.js` 中的中文文件名缺少序号前缀
- `draft` 命名为"脚本撰写"，与文档不一致

**修复**:
- 所有中文阶段文件添加序号前缀（01., 02., 03...）
- 统一 `draft` 为"脚本草稿"

**影响文件**:
- `v1.0/lib/utils/i18n.js` (第 332-338 行)

**修改内容**:
```javascript
const FILE_NAMES = {
  zh: {
    idea: '01.选题沟通.md',       // ✅ 添加序号
    frame: '02.框架搭建.md',
    research: '03.内容调研.md',
    outline: '04.大纲确认.md',
    draft: '05.脚本草稿.md',      // ✅ 统一命名
    script: '最终脚本.md',
  },
  // en 保持不变
}
```

---

## 📁 文件命名对照表

### 中文工作空间 (dirLang: "zh")

| 阶段 | 文件名 | 说明 |
|------|--------|------|
| 阶段1 | `01.选题沟通.md` | 带序号前缀 |
| 阶段2 | `02.框架搭建.md` | 带序号前缀 |
| 阶段3 | `03.内容调研.md` | 带序号前缀 |
| 阶段4 | `04.大纲确认.md` | 带序号前缀 |
| 阶段5 | `05.脚本草稿.md` | 带序号前缀 + 统一命名 |
| 最终 | `最终脚本.md` | 不带序号 |

### 英文工作空间 (dirLang: "en")

| Stage | File Name | Note |
|-------|-----------|------|
| Stage 1 | `idea.md` | No prefix |
| Stage 2 | `frame.md` | No prefix |
| Stage 3 | `research.md` | No prefix |
| Stage 4 | `outline.md` | No prefix |
| Stage 5 | `draft.md` | No prefix |
| Final | `script.md` | No prefix |

---

## 🔧 技术架构

### 实现方式

**v1.0.7 = Prompt 驱动 + project-manager skill**

- ✅ Agent 通过 `project-manager` skill 管理项目
- ✅ Skill 返回配置（dirNames、fileNames）
- ✅ Agent 使用配置进行文件操作
- ✅ `i18n.js` 提供文件名映射

### 工作流程

```
用户描述选题
    ↓
Agent 调用 project-manager skill
    ↓
Skill 检测/创建项目
    ↓
Skill 返回配置（包含 fileNames）
    ↓
Agent 使用 config.fileNames 创建文件
    ↓
文件名正确（带序号）
```

---

## 📦 安装验证

### 测试新初始化

```bash
# 创建新测试目录
mkdir my-test-workspace
cd my-test-workspace

# 初始化工作空间
npx @waoooo/genkicap-workflow@1.0.7

# 验证 skills 是否安装
ls -la .claude/skills/project-manager/
```

**预期结果**:
```
.claude/
├── CLAUDE.md
├── template/
└── skills/
    └── project-manager/
        ├── SKILL.md
        └── references/
```

---

## 🐛 已知问题修复

### 问题 1: Skill 调用失败

**症状**: Agent 调用 skill 时报错 "Unknown skill: project-manager"

**根本原因**: `init.js` 没有复制 `.claude/skills/` 目录

**修复状态**: ✅ 已修复

---

### 问题 2: _meta.json 数据不同步

**症状**: 手动创建的项目导致 `scripts/_meta.json` 的 `total_projects` 为 0，但实际有项目

**根本原因**: 项目不是通过 skill 创建的

**解决方案**:
- ✅ 确保 skill 正确安装
- ✅ Agent 必须通过 skill 创建项目
- 📝 用户需重新初始化工作空间或手动复制 skills

---

## 🔄 升级指南

### 对于现有用户

**选项 1: 重新初始化（推荐）**

```bash
# 备份现有项目
cp -r 脚本/ 脚本_backup/

# 重新初始化
npx @waoooo/genkicap-workflow@1.0.7
```

**选项 2: 手动复制 skills**

```bash
# 从 v1.0 源码复制
cp -r /path/to/Video-workflow/v1.0/.claude/skills/ .claude/
```

---

## 📝 文档更新

### CLAUDE_CN.md

- ✅ 更新版本号: v1.0.6 → v1.0.7
- ✅ 更新实现方式说明
- ✅ 确认文件命名规范正确
- ✅ 更新最后更新日期: 2025-12-15

---

## 🎉 总结

**v1.0.7 修复了两个关键问题**:

1. ✅ **Skill 安装问题** - 用户初始化后 skill 可以正常使用
2. ✅ **文件命名规范** - 中文文件名带序号，命名统一

**架构升级**:
- v1.0.6: 纯 Prompt 驱动（理论上）
- v1.0.7: Prompt + project-manager skill（实际实现）

**下一步计划**:
- [ ] 测试验证 v1.0.7
- [ ] 发布到 npm
- [ ] 更新用户文档

---

**变更日期**: 2025-12-15
**责任人**: Claude Code
