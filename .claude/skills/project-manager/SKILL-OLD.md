---
name: project-manager
description: 检测视频脚本工作空间配置和项目状态，根据检测结果引导用户创建新项目或选择现有项目，返回 CURRENT_PROJECT 路径供后续工作流使用。适用于用户说"开始创作"、"新建项目"、"继续项目"或直接描述选题内容时。
model: claude-sonnet-4-5
---

# Project Manager

视频脚本项目检测和管理，确保工作流在正确的项目上下文中执行。

## 核心职责

1. 检测工作空间配置（config.json）
2. 检测项目目录状态（scripts/ 或 脚本/）
3. 根据检测结果引导用户操作
4. 返回 CURRENT_PROJECT 路径供后续使用

## 调用时机

**在用户开始创作工作流之前，必须先调用此 skill。**

触发条件：
- 用户说"开始创作"、"新建项目"、"继续项目"
- 用户直接描述选题内容（如："我想做一个关于AI的视频"）
- 对话开始时，主动调用检测项目状态

## 执行流程

### 步骤 1: 检测工作空间配置

检查 config.json 是否存在：

```bash
ls config.json 2>&1
```

**判断结果**：

**情况 A：未找到配置文件**（输出包含 "No such file"）

→ 提示用户初始化工作空间，停止执行：

```
❌ 未检测到工作空间配置 (config.json)

请先运行以下命令初始化工作空间:
npx @waoooo/genkicap-workflow

初始化后再继续。
```

**情况 B：配置文件存在**（输出是 "config.json"）

→ 继续步骤 2

### 错误处理清单

**工作空间检测失败**:
- config.json 不存在 → 提示运行 `npx @waoooo/genkicap-workflow`
- config.json 格式错误 → 提示检查 JSON 格式，重新初始化
- 缺少必要字段 (dirLang, dirNames) → 提示重新初始化工作空间

**项目创建失败**:
- 项目目录已存在 → 提示更换名称或删除现有项目
- 目录权限不足 → 提示检查文件系统权限
- 磁盘空间不足 → 提示清理磁盘空间
- _meta.json 写入失败 → 提示检查权限，清理未完成的项目

---

### 步骤 2: 读取工作空间配置

读取配置文件：

```bash
cat config.json
```

**提取关键字段**：
- `dirLang`: 目录语言（"zh" 或 "en"）
- `aiLang`: AI 输出语言（"zh" 或 "en"）
- `dirNames`: 目录名称映射对象
- `fileNames`: 文件名称映射对象

**确定项目目录名称**：

```javascript
// 从 config.json 读取
const scriptsDir = dirNames.scripts;
// 如果 dirLang="zh" → "脚本"
// 如果 dirLang="en" → "scripts"
```

保存到上下文：`SCRIPTS_DIR = {scriptsDir}`

---

### 步骤 3: 检测项目目录状态

检查项目目录：

```bash
ls {SCRIPTS_DIR}/ 2>&1
```

**判断结果**：

#### 情况 A：目录不存在或为空

输出包含 "No such file" 或只有 "total 0"

→ 引导用户创建新项目：

```
🎬 欢迎使用视频脚本创作助手！

我检测到你还没有创建项目。

请提供以下信息创建新项目：

1. 项目名称（支持中文）:
   示例: "AI工具测评" 或 "tech-review"

2. 项目描述（可选）:
   示例: "对比主流AI写作工具的优劣"
```

收集信息后 → 跳转到**步骤 4: 创建新项目**

---

#### 情况 B：存在 1 个项目

统计项目数量：

```bash
ls {SCRIPTS_DIR}/ 2>&1 | grep -v "^total" | wc -l
```

输出是 `1` → 只有一个项目

读取项目元数据：

```bash
PROJECT_NAME=$(ls {SCRIPTS_DIR}/ | head -1)
cat "{SCRIPTS_DIR}/$PROJECT_NAME/_meta.json"
```

展示项目信息：

```
🎬 欢迎回来！

我检测到项目: {name}
描述: {description}
当前阶段: {metadata.stage} - {当前阶段的 display_name}
进度: {progress}%
最后更新: {updated_at}

是否继续在此项目中工作？
1. 是 - 继续当前项目
2. 否 - 创建新项目
```

**用户选择**：
- 选择"是" → 设置 `CURRENT_PROJECT = "{SCRIPTS_DIR}/{project_id}"` 并返回
- 选择"否" → 跳转到**步骤 4: 创建新项目**

---

#### 情况 C：存在多个项目

统计项目数量：

```bash
ls {SCRIPTS_DIR}/ 2>&1 | grep -v "^total" | wc -l
```

输出 > 1 → 存在多个项目

读取所有项目元数据：

```bash
ls {SCRIPTS_DIR}/

for project in $(ls {SCRIPTS_DIR}/); do
  cat "{SCRIPTS_DIR}/$project/_meta.json"
done
```

展示项目列表：

```
🎬 欢迎回来！

我检测到你有 {count} 个项目：

1. {project_1.name} ({project_1.project_id})
   描述: {project_1.description}
   阶段: {project_1.stage} | 进度: {project_1.progress}%

2. {project_2.name} ({project_2.project_id})
   描述: {project_2.description}
   阶段: {project_2.stage} | 进度: {project_2.progress}%

请选择：
- 输入项目编号（1-{count}）继续现有项目
- 输入 "new" 创建新项目
```

**用户选择**：
- 输入编号 → 设置 `CURRENT_PROJECT = "{SCRIPTS_DIR}/{选中的project_id}"` 并返回
- 输入 "new" → 跳转到**步骤 4: 创建新项目**

---

### 步骤 4: 创建新项目

**收集项目信息**：

询问用户：
```
请提供以下信息：

1. 项目名称（支持中文）:
2. 项目描述（可选）:
```

**生成项目 ID**：

```javascript
// 生成日期前缀 YYYYMMDD
const date = new Date().toISOString().split('T')[0].replace(/-/g, '');

// 生成完整项目 ID: date-name 格式
const project_id = `${date}-${projectName.trim()}`;

// 示例:
// 用户输入: "AI工具测评"
// 生成: "20251214-AI工具测评"
```

**检查项目是否已存在**：

```bash
ls "{SCRIPTS_DIR}/{project_id}" 2>&1
```

- 输出不包含 "No such file" → 项目已存在，提示用户更换名称
- 输出包含 "No such file" → 可以创建，继续

**创建目录结构**：

从 config.json 读取 dirNames，动态创建目录：

```bash
# 使用 dirNames 映射动态创建
mkdir -p "{SCRIPTS_DIR}/{project_id}/{dirNames.stages}"
mkdir -p "{SCRIPTS_DIR}/{project_id}/{dirNames.contexts}/{dirNames.research}"
mkdir -p "{SCRIPTS_DIR}/{project_id}/{dirNames.contexts}/{dirNames.videos}"
mkdir -p "{SCRIPTS_DIR}/{project_id}/{dirNames.contexts}/{dirNames.channels}"
mkdir -p "{SCRIPTS_DIR}/{project_id}/_归档"

# 示例效果 (dirLang="zh"):
# → 脚本/20251214-AI工具测评/阶段/
# → 脚本/20251214-AI工具测评/上下文/调研/
# → 脚本/20251214-AI工具测评/上下文/视频/
# → 脚本/20251214-AI工具测评/上下文/账号/
# → 脚本/20251214-AI工具测评/_归档/

# 示例效果 (dirLang="en"):
# → scripts/20251214-AI-review/stages/
# → scripts/20251214-AI-review/contexts/research/
# → scripts/20251214-AI-review/contexts/videos/
# → scripts/20251214-AI-review/contexts/channels/
# → scripts/20251214-AI-review/_archives/
```

**创建 _meta.json**：

使用 Write 工具创建 `{SCRIPTS_DIR}/{project_id}/_meta.json`

详见：[references/meta-structure.md](references/meta-structure.md)

**创建 _context.md**：

使用 Write 工具创建 `{SCRIPTS_DIR}/{project_id}/_context.md`

```markdown
# {project_id}

## 项目信息

- **创建时间**: {date}
- **项目描述**: {description}

## 备注

（用户可以在此添加项目相关的备注和说明）
```

**确认创建成功**：

```
✅ 项目创建成功！

项目名称: {name}
项目 ID: {project_id}
项目路径: {SCRIPTS_DIR}/{project_id}/

现在可以开始第一阶段：选题沟通。
```

---

### 步骤 5: 返回结果

**设置 CURRENT_PROJECT 变量**：

在整个对话过程中保存当前项目路径：

```javascript
CURRENT_PROJECT = "{SCRIPTS_DIR}/{project_id}"

// 示例:
// CURRENT_PROJECT = "脚本/20251214-AI工具测评"
// 或
// CURRENT_PROJECT = "scripts/20251214-AI工具测评"
```

**重要**：后续所有文件操作都使用此前缀

- ✅ `{CURRENT_PROJECT}/阶段/选题沟通.md`
- ✅ `{CURRENT_PROJECT}/_meta.json`
- ✅ `{CURRENT_PROJECT}/_归档/选题沟通_v01.md`
- ❌ 绝对不要直接使用 `阶段/选题沟通.md`

**返回给调用方**：

```json
{
  "success": true,
  "project_id": "20251214-AI工具测评",
  "project_path": "脚本/20251214-AI工具测评",
  "config": {
    "dirLang": "zh",
    "aiLang": "zh",
    "dirNames": {...},
    "fileNames": {...}
  }
}
```

---

## 重要约束

### 项目命名规则

- ✅ **正确**: `YYYYMMDD-项目名`（日期在前）
  - `20251214-AI工具测评`
  - `20251214-tech-review`

- ❌ **错误**: `项目名-YYYYMMDD`（日期在后）

### 字段命名规范

_meta.json 使用 **snake_case**:
- `project_id` ✅
- `created_at` ✅
- `updated_at` ✅
- `workflow_type` ✅

### 与 npm 包保持一致

此 skill 严格参考:
- `v1.0/lib/utils/config.js` - findScriptsDir(), getWorkspaceConfig()
- `v1.0/lib/modules/project.js` - createProject()
- `v1.0/lib/utils/i18n.js` - getDirName(), getFileName()

确保 Agent 行为与 CLI 工具完全一致。

---

## 参考文档

- [_meta.json 结构说明](references/meta-structure.md)
- [config.json 配置说明](references/config-structure.md)

---

**版本**: v2.1.0
**最后更新**: 2025-12-14
**变更说明**:
- v2.1.0: 修复目录创建示例硬编码问题，补充错误处理，完善文档注意事项
- v2.0.0: 重构为标准 Skills 格式，简化逻辑，添加 references 支持
